CREATE DATABASE db_barang_bukti;
USE db_barang_bukti;

CREATE TABLE barang_bukti (
    id INT AUTO_INCREMENT PRIMARY KEY,
    jenis_barang VARCHAR(100) NOT NULL,
    nama_barang VARCHAR(150) NOT NULL,
    jumlah INT NOT NULL,
    foto VARCHAR(255) NOT NULL
);
